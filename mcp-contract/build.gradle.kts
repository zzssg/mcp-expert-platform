// Pure-Java contract module: DTOs, envelope, enums. No logic, no frameworks,
// so it can be consumed by any module and serialized by Jackson without
// requiring Jackson annotations (record component names map directly).
plugins { `java-library` }
