package com.bank.platform.mcp.contract;

/**
 * Retained from the full design (Part 2.3). In this RAG-excluded scope every
 * descriptor uses {@link #NONE}; OPTIONAL/REQUIRED are reserved for a future
 * rag-core module and are not wired anywhere here.
 */
public enum RagProfile { NONE, OPTIONAL, REQUIRED }
